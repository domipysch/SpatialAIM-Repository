name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0 # setuptools-scm needs history for the version
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: "3.13"
      - name: Install lint tools
        run: pip install black mypy types-PyYAML
      - name: Install package without deps so `spatialaim` is importable for mypy
        run: pip install -e . --no-deps
      - name: black
        run: black --check .
      - name: mypy
        run: mypy src

  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: "3.13"
      - name: Install package + dev deps
        run: pip install -e .[dev]
      - name: Run fast tests
        run: pytest -m "not slow"

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: "3.13"
      - name: Build sdist + wheel
        run: |
          pip install build twine
          python -m build
          twine check dist/*
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: dist
          path: dist/*

  integration-spatialaim:
    # End-to-end SpatialAIM sweep on the sample dataset in spatialaim_env.
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -el {0}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - uses: conda-incubator/setup-miniconda@8ee1f361103df19b6f8c8655fd3967a8ecb162d5 # v4
        with:
          activate-environment: spatialaim_env
          environment-file: environment.yml
          auto-activate-base: false
          miniforge-version: latest
          cache-environment: true
      - name: Install package (no deps; env already has them)
        run: pip install -e . --no-deps
      - name: spatialaim run — single pair (nearest_centroid)
        run: |
          spatialaim run \
            --scdata     sample_dataset/scRNA/sample_sc.h5ad \
            --stdata     sample_dataset/ST/sample_st.h5ad \
            --output_dir sample_output/novel/nearest_centroid \
            --mapping    nearest_centroid --leiden_resolution 3.0
      - name: spatialaim run — single pair (wann)
        run: |
          spatialaim run \
            --scdata     sample_dataset/scRNA/sample_sc.h5ad \
            --stdata     sample_dataset/ST/sample_st.h5ad \
            --output_dir sample_output/novel/wann \
            --mapping    wann --leiden_resolution 3.0
      - name: spatialaim run — batch (all pairs)
        run: |
          spatialaim run \
            --pairs_csv  sample_dataset/pairs.csv \
            --sc_dir     sample_dataset/scRNA \
            --st_dir     sample_dataset/ST \
            --output_dir sample_output/novel/sample_batch \
            --mapping    nearest_centroid --leiden_resolution 3.0
      - name: Slow / integration tests (sample data)
        run: pytest -m slow

  integration-reference:
    # Reference-aligner path: spatialaim_env orchestrates the sweep (single + batch) with
    # `--mapping tangram`, so ReferenceMapper drives an AlignerWorker and
    # tangram_env runs the aligner via `conda run`, once per K.
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -el {0}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      - uses: conda-incubator/setup-miniconda@8ee1f361103df19b6f8c8655fd3967a8ecb162d5 # v4
        with:
          activate-environment: spatialaim_env
          environment-file: environment.yml
          auto-activate-base: false
          miniforge-version: latest
          cache-environment: true
      - name: Create tangram_env
        run: conda env create -f src/spatialaim/reference_aligners/environment_tangram.yml
      - name: Install package into both envs (no deps)
        run: |
          pip install -e . --no-deps
          conda run -n tangram_env pip install -e . --no-deps
      - name: spatialaim run — single pair (tangram)
        run: |
          spatialaim run --mapping tangram \
            --scdata     sample_dataset/scRNA/sample_sc.h5ad \
            --stdata     sample_dataset/ST/sample_st.h5ad \
            --output_dir sample_output/tangram/sample
      - name: spatialaim run — batch (tangram)
        run: |
          spatialaim run --mapping tangram \
            --pairs_csv  sample_dataset/pairs.csv \
            --sc_dir     sample_dataset/scRNA \
            --st_dir     sample_dataset/ST \
            --output_dir sample_output/tangram/sample_batch
